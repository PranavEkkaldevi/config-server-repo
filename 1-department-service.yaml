server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/1_department_service_db
    username: root
    password: root

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update

eureka:
  instance:
    client:
      serverUrl:
        defaultZone: http://localhost:8761/eureka/

message: "Hello, department service updated"
